# Staging Security Configuration
security.encryption.enabled=true
security.authentication.required=true
security.cors.enabled=true
security.cors.allowed_origins=https://staging.transcendent.com
security.rate_limiting.enabled=true
security.rate_limiting.requests_per_minute=1000
security.jwt.secret=${STAGING_JWT_SECRET}
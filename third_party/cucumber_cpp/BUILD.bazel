load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "cucumber_cpp",
    hdrs = glob(["include/**/*.hpp"]),
    srcs = glob(["src/**/*.cpp"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    deps = [
        "@gtest//:gtest",
    ],
)
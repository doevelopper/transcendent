apiVersion: v1
kind: Service
metadata:
  name: {{ include "transcendent.fullname" . }}
  labels:
    {{- include "transcendent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transcendent.selectorLabels" . | nindent 4 }}